function scrollToDescription() {
    // Encuentra la sección de descripción usando su ID
    const descripcion = document.querySelector('#descripcion');
    // Usa la función scrollIntoView() para hacer scroll hasta la sección
    descripcion.scrollIntoView({ behavior: 'smooth' });
  }
  function scrollToDescription2() {
    // Encuentra la sección de descripción usando su ID
    const descripcion = document.querySelector('#descripcion2');
    // Usa la función scrollIntoView() para hacer scroll hasta la sección
    descripcion.scrollIntoView({ behavior: 'smooth' });
  }
  function scrollToDescription3() {
    // Encuentra la sección de descripción usando su ID
    const descripcion = document.querySelector('#descripcion3');
    // Usa la función scrollIntoView() para hacer scroll hasta la sección
    descripcion.scrollIntoView({ behavior: 'smooth' });
  }


  